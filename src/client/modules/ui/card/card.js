import { LightningElement } from 'lwc';

export default class Card extends LightningElement {
    // No logic needed here since we are just using <slot>, 
    // but this file is required for the component to exist.
}